{
  "name": "magus2-backend",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^8.4.1",
    "express": "^4.21.1"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^1.7.6",
    "@sap/cds-dk": "^8.4.1",
    "@sap/eslint-plugin-cds": "^3.1.2",
    "eslint": "^9.14.0"
  },
  "scripts": {
    "start": "cds-serve",
    "dev": "cds deploy && cds watch"
  },
  "installConfig": {
    "pnp": true
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "url": "db.sqlite"
        }
      }
    }
  }
}
